(()=>{const params=new URLSearchParams(location.hash.slice(1));const token=params.get('token');const target=params.get('return_to')||'/';history.replaceState(null,'',location.pathname);if(!token){document.querySelector('#state').textContent='OIDC 登入未返回有效權杖。';return}sessionStorage.setItem('farhp_staff_token',token);location.replace(target.startsWith('/')&&!target.startsWith('//')?target:'/')})();
