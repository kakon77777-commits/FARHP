const assert=require('assert');global.window={};require('../data/EMPSL_atom_registry_v0.2.js');const R=window.EMPSL_ATOM_REGISTRY;const C=require('../assets/empsl_core.js');assert.equal(R.count,128);assert.equal(new Set(R.atoms.map(a=>a.id)).size,128);const p={frame:'SEM-ENTITY',seed:'ENO-07',phonology:['ONSET-G','HU-HEKOU','RIME-ANG'],tone:'T1',phase:'PH16-05',operator:'OP-NULL'};assert(C.validateRecipe(p,R,{}).ok);const svg=C.composeSvg(p,R);assert(svg.includes('<svg'));assert(svg.includes('viewBox="0 0 512 512"'));C.recipeHash(p).then(h=>{assert.equal(h.length,64);console.log('PASS EMPSL core tests · 6 groups');});